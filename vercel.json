{
  "buildCommand": "npm run build -w client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://api.rotissorie.com/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
